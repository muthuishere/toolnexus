;; S16 — cljgo side. Same source, same koine artifact from Clojars.
(defn build [b]
  (dep b "net.clojars.muthuishere/koine" {:mvn/version "0.7.3"})
  (let [app (exe b {:name "loopslice" :main "src/toolnexus/loopslice.cljc"})]
    (install b app)
    (run b app)))
