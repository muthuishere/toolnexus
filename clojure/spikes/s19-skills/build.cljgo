;; S19 — cljgo side. THE SAME src/toolnexus/skills.cljc, the same koine artifact
;; from Clojars. No vendored copy, no git coordinate, no reader conditional in
;; toolnexus' own source.
;;
;;   cljgo build        compile to a native binary
;;   cljgo build run    ...and run it
(defn build [b]
  (dep b "net.clojars.muthuishere/koine" {:mvn/version "0.9.0"})
  (let [app (exe b {:name "skills" :main "src/toolnexus/skills.cljc"})]
    (install b app)
    (run b app)))
