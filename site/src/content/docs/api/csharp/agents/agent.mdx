---
title: "A2A.Agent"
description: "Point at a remote agent's card and use it exactly like a local tool. — C#."
sidebar:
  label: "A2A.Agent"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>C#</strong> · package <code>Toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7A</a>
</p>

```csharp
{/* TODO:signature */}
```

Point at a remote agent's card and use it exactly like a local tool.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```csharp
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```csharp
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```csharp
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `agent` | [→](/api/javascript/agents/agent/) |
| Python | `agent` | [→](/api/python/agents/agent/) |
| Go | `agents.New` | [→](/api/go/agents/agent/) |
| Java | `A2A.agent` | [→](/api/java/agents/agent/) |
| Elixir | `Toolnexus.Agents.new` | [→](/api/elixir/agents/agent/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`A2A.AgentTools`](/api/csharp/agents/agent-tools/) — Expand a remote agent card into one tool per advertised skill.
- [`A2A.ParseAgentsConfig`](/api/csharp/agents/parse-config/) — Declare remote peers in config the way MCP servers are declared, with precedence rules.
