---
title: "Compaction.Compactor"
description: "Summarise older turns when the transcript grows, so a long-lived agent stays inside its context window. — C#."
sidebar:
  label: "Compaction.Compactor"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>C#</strong> · package <code>Toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7F</a>
</p>

```csharp
{/* TODO:signature */}
```

Summarise older turns when the transcript grows, so a long-lived agent stays inside its context window.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```csharp
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```csharp
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```csharp
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `compactor` | [→](/api/javascript/compaction/compactor/) |
| Python | `compactor` | [→](/api/python/compaction/compactor/) |
| Go | `Compactor` | [→](/api/go/compaction/compactor/) |
| Java | `Compaction.compactor` | [→](/api/java/compaction/compactor/) |
| Elixir | `Toolnexus.Agents.Compaction.compactor` | [→](/api/elixir/compaction/compactor/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- _TODO: related entry points_
