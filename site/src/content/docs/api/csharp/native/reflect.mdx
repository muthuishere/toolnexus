---
title: "ToolMethodAttribute"
description: "Derive the schema from the function signature or annotation instead of writing it by hand. — C#."
sidebar:
  label: "ToolMethodAttribute"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>C#</strong> · package <code>Toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §6</a>
</p>

```csharp
{/* TODO:signature */}
```

Derive the schema from the function signature or annotation instead of writing it by hand.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```csharp
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```csharp
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```csharp
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `tool` | [→](/api/javascript/native/reflect/) |
| Python | `tool` | [→](/api/python/native/reflect/) |
| Go | `NativeToolReflect` | [→](/api/go/native/reflect/) |
| Java | `@Tool annotation` | [→](/api/java/native/reflect/) |
| Elixir | _not shipped in this port_ |  |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`NativeTool.Of`](/api/csharp/native/define-tool/) — Wrap a plain function with a name, description and schema — the shortest path from code you have to a tool the LLM can call.
- [`Tools.Collect`](/api/csharp/native/collect/) — Sweep a module or class and collect every function marked as a tool.
