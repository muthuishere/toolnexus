---
title: "Suspension.AuthRequired"
description: "The auth-shaped suspension: hand back a URL, resume once the user has granted access. — C#."
sidebar:
  label: "Suspension.AuthRequired"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>C#</strong> · package <code>Toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §10</a>
</p>

```csharp
{/* TODO:signature */}
```

The auth-shaped suspension: hand back a URL, resume once the user has granted access.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```csharp
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```csharp
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```csharp
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `authRequired` | [→](/api/javascript/suspension/auth-required/) |
| Python | `auth_required` | [→](/api/python/suspension/auth-required/) |
| Go | `AuthRequired` | [→](/api/go/suspension/auth-required/) |
| Java | `Request.authRequired` | [→](/api/java/suspension/auth-required/) |
| Elixir | _not shipped in this port_ |  |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Suspension.Pending`](/api/csharp/suspension/pending/) — Return a Pending from a tool to park the run until someone answers.
- [`LlmClient.WaitFor`](/api/csharp/suspension/wait-for/) — The single hook where the host resolves a suspension — in-process prompt or durable queue, same contract.
- [`Suspension.PendingOf`](/api/csharp/suspension/pending-of/) — Detect that a RunResult is parked rather than finished, and get the Request that parked it.
- [`LlmClient.ResumeAsync`](/api/csharp/suspension/resume/) — The answer-carrying entry point: resume a parked run in a different process.
