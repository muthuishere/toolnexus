---
title: "SkillSource.ListSkills"
description: "Enumerate discovered skills and, crucially, the ones that were skipped and why. — C#."
sidebar:
  label: "SkillSource.ListSkills"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>C#</strong> · package <code>Toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §3</a>
</p>

```csharp
{/* TODO:signature */}
```

Enumerate discovered skills and, crucially, the ones that were skipped and why.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```csharp
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```csharp
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```csharp
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `listSkills` | [→](/api/javascript/skills/list/) |
| Python | `list_skills` | [→](/api/python/skills/list/) |
| Go | `ListSkills` | [→](/api/go/skills/list/) |
| Java | `SkillSource.listSkills` | [→](/api/java/skills/list/) |
| Elixir | `Toolnexus.Skill.list` | [→](/api/elixir/skills/list/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`SkillSource.Load`](/api/csharp/skills/load/) — Glob a skills directory and expose one skill tool with progressive disclosure.
- [`SkillSource.LoadWith`](/api/csharp/skills/load-with/) — Supply skills as data or from a lazy provider, and narrow them per agent with an allowlist.
