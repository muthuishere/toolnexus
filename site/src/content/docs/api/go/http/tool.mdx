---
title: "HTTPTool"
description: "Turn an HTTP endpoint into a tool: URL, method, headers with ${ENV} expansion, and a schema. — Go."
sidebar:
  label: "HTTPTool"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7</a>
</p>

```go
{/* TODO:signature */}
```

Turn an HTTP endpoint into a tool: URL, method, headers with $\{ENV\} expansion, and a schema.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `httpTool` | [→](/api/javascript/http/tool/) |
| Python | `http_tool` | [→](/api/python/http/tool/) |
| Java | `HttpTool.of` | [→](/api/java/http/tool/) |
| C# | `HttpTool.Of` | [→](/api/csharp/http/tool/) |
| Elixir | `Toolnexus.Http.tool` | [→](/api/elixir/http/tool/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- _TODO: related entry points_
