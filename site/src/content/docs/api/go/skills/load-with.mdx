---
title: "LoadSkillsWith"
description: "Supply skills as data or from a lazy provider, and narrow them per agent with an allowlist. — Go."
sidebar:
  label: "LoadSkillsWith"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §3</a>
</p>

```go
{/* TODO:signature */}
```

Supply skills as data or from a lazy provider, and narrow them per agent with an allowlist.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `loadSkills` | [→](/api/javascript/skills/load-with/) |
| Python | `load_skills` | [→](/api/python/skills/load-with/) |
| Java | `SkillSource.loadWith` | [→](/api/java/skills/load-with/) |
| C# | `SkillSource.LoadWith` | [→](/api/csharp/skills/load-with/) |
| Elixir | `Toolnexus.Skill.load` | [→](/api/elixir/skills/load-with/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`LoadSkills`](/api/go/skills/load/) — Glob a skills directory and expose one skill tool with progressive disclosure.
- [`ListSkills`](/api/go/skills/list/) — Enumerate discovered skills and, crucially, the ones that were skipped and why.
