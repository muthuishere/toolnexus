---
title: "ParseAgentsConfig"
description: "Declare remote peers in config the way MCP servers are declared, with precedence rules. — Go."
sidebar:
  label: "ParseAgentsConfig"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7A</a>
</p>

```go
{/* TODO:signature */}
```

Declare remote peers in config the way MCP servers are declared, with precedence rules.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `parseAgentsConfig` | [→](/api/javascript/agents/parse-config/) |
| Python | `parse_agents_config` | [→](/api/python/agents/parse-config/) |
| Java | `A2A.parseAgentsConfig` | [→](/api/java/agents/parse-config/) |
| C# | `A2A.ParseAgentsConfig` | [→](/api/csharp/agents/parse-config/) |
| Elixir | `Toolnexus.Agents.parse_config` | [→](/api/elixir/agents/parse-config/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`agents.New`](/api/go/agents/agent/) — Point at a remote agent's card and use it exactly like a local tool.
- [`AgentTools`](/api/go/agents/agent-tools/) — Expand a remote agent card into one tool per advertised skill.
