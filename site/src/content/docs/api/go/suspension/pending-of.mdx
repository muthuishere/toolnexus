---
title: "PendingOf"
description: "Detect that a RunResult is parked rather than finished, and get the Request that parked it. — Go."
sidebar:
  label: "PendingOf"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §10</a>
</p>

```go
{/* TODO:signature */}
```

Detect that a RunResult is parked rather than finished, and get the Request that parked it.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `pendingOf` | [→](/api/javascript/suspension/pending-of/) |
| Python | `pending_of` | [→](/api/python/suspension/pending-of/) |
| Java | `Request.pendingOf` | [→](/api/java/suspension/pending-of/) |
| C# | `Suspension.PendingOf` | [→](/api/csharp/suspension/pending-of/) |
| Elixir | `Toolnexus.Request.pending_of` | [→](/api/elixir/suspension/pending-of/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Pending`](/api/go/suspension/pending/) — Return a Pending from a tool to park the run until someone answers.
- [`AuthRequired`](/api/go/suspension/auth-required/) — The auth-shaped suspension: hand back a URL, resume once the user has granted access.
- [`WaitFor`](/api/go/suspension/wait-for/) — The single hook where the host resolves a suspension — in-process prompt or durable queue, same contract.
- [`Client.Resume`](/api/go/suspension/resume/) — The answer-carrying entry point: resume a parked run in a different process.
