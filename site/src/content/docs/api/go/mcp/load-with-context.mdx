---
title: "LoadMcpWithContext"
description: "The ctx-aware load: bound connection time and cancel a slow or hung server without leaking a child process. — Go."
sidebar:
  label: "LoadMcpWithContext"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §2</a>
</p>

```go
{/* TODO:signature */}
```

The ctx-aware load: bound connection time and cancel a slow or hung server without leaking a child process.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `loadMcp` | [→](/api/javascript/mcp/load-with-context/) |
| Python | `load_mcp_with_context` | [→](/api/python/mcp/load-with-context/) |
| Java | `McpSource.loadWith` | [→](/api/java/mcp/load-with-context/) |
| C# | `McpSource.LoadAsync` | [→](/api/csharp/mcp/load-with-context/) |
| Elixir | `Toolnexus.Mcp.load` | [→](/api/elixir/mcp/load-with-context/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`LoadMcp`](/api/go/mcp/load/) — Read an mcp.json, connect every local stdio and remote streamable-HTTP server, expose each server tool as a Tool.
- [`ListMcpTools`](/api/go/mcp/list-tools/) — List what each configured server would expose, plus per-server status, without wiring it into a toolkit.
- [`ParseMcpConfig`](/api/go/mcp/parse-config/) — Parse and validate config without connecting — the fast fail for a malformed or misspelled server block.
- [`ElicitationToRequest`](/api/go/mcp/elicitation/) — Map an MCP server's elicitation request onto the §10 suspension contract, and map the answer back.
