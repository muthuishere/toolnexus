---
title: "NativeTool"
description: "Wrap a plain function with a name, description and schema — the shortest path from code you have to a tool the LLM can call. — Go."
sidebar:
  label: "NativeTool"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §6</a>
</p>

```go
{/* TODO:signature */}
```

Wrap a plain function with a name, description and schema — the shortest path from code you have to a tool the LLM can call.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `defineTool` | [→](/api/javascript/native/define-tool/) |
| Python | `define_tool` | [→](/api/python/native/define-tool/) |
| Java | `NativeTool.of` | [→](/api/java/native/define-tool/) |
| C# | `NativeTool.Of` | [→](/api/csharp/native/define-tool/) |
| Elixir | `Toolnexus.Native.define` | [→](/api/elixir/native/define-tool/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`NativeToolReflect`](/api/go/native/reflect/) — Derive the schema from the function signature or annotation instead of writing it by hand.
