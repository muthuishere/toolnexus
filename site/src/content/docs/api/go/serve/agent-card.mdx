---
title: "BuildAgentCard"
description: "Construct the Agent Card that advertises your name, skills and endpoint. — Go."
sidebar:
  label: "BuildAgentCard"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Go</strong> · package <code>github.com/muthuishere/toolnexus/golang</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7B</a>
</p>

```go
{/* TODO:signature */}
```

Construct the Agent Card that advertises your name, skills and endpoint.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```go
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```go
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```go
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `buildAgentCard` | [→](/api/javascript/serve/agent-card/) |
| Python | `build_agent_card` | [→](/api/python/serve/agent-card/) |
| Java | `A2AServer.buildAgentCard` | [→](/api/java/serve/agent-card/) |
| C# | `A2AServer.BuildAgentCard` | [→](/api/csharp/serve/agent-card/) |
| Elixir | `Toolnexus.Serve.build_agent_card` | [→](/api/elixir/serve/agent-card/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`StartAgent`](/api/go/serve/a2a/) — Publish an Agent Card and answer JSON-RPC over the client loop — your toolkit becomes someone else's remote agent.
- [`NewFileTaskStore`](/api/go/serve/task-store/) — Persist inbound A2A tasks so a suspended request survives a restart.
- [`ExposedMcpTools`](/api/go/serve/mcp/) — The inbound MCP profile: any MCP client can call your tools.
