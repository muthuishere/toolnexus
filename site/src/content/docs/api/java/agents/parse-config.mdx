---
title: "A2A.parseAgentsConfig"
description: "Declare remote peers in config the way MCP servers are declared, with precedence rules. — Java."
sidebar:
  label: "A2A.parseAgentsConfig"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Java</strong> · package <code>io.github.muthuishere:toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7A</a>
</p>

```java
{/* TODO:signature */}
```

Declare remote peers in config the way MCP servers are declared, with precedence rules.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```java
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```java
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```java
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `parseAgentsConfig` | [→](/api/javascript/agents/parse-config/) |
| Python | `parse_agents_config` | [→](/api/python/agents/parse-config/) |
| Go | `ParseAgentsConfig` | [→](/api/go/agents/parse-config/) |
| C# | `A2A.ParseAgentsConfig` | [→](/api/csharp/agents/parse-config/) |
| Elixir | `Toolnexus.Agents.parse_config` | [→](/api/elixir/agents/parse-config/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`A2A.agent`](/api/java/agents/agent/) — Point at a remote agent's card and use it exactly like a local tool.
- [`A2A.agentTools`](/api/java/agents/agent-tools/) — Expand a remote agent card into one tool per advertised skill.
