---
title: "McpServe.build"
description: "The inbound MCP profile: any MCP client can call your tools. — Java."
sidebar:
  label: "McpServe.build"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Java</strong> · package <code>io.github.muthuishere:toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7C</a>
</p>

```java
{/* TODO:signature */}
```

The inbound MCP profile: any MCP client can call your tools.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```java
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```java
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```java
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `buildMcpServer` | [→](/api/javascript/serve/mcp/) |
| Python | `build_mcp_server` | [→](/api/python/serve/mcp/) |
| Go | `ExposedMcpTools` | [→](/api/go/serve/mcp/) |
| C# | `McpServe.Build` | [→](/api/csharp/serve/mcp/) |
| Elixir | `Toolnexus.McpServe.build` | [→](/api/elixir/serve/mcp/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`A2AServer.start`](/api/java/serve/a2a/) — Publish an Agent Card and answer JSON-RPC over the client loop — your toolkit becomes someone else's remote agent.
- [`A2AServer.buildAgentCard`](/api/java/serve/agent-card/) — Construct the Agent Card that advertises your name, skills and endpoint.
- [`A2AServer.FileTaskStore`](/api/java/serve/task-store/) — Persist inbound A2A tasks so a suspended request survives a restart.
