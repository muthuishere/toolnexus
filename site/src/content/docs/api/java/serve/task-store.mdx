---
title: "A2AServer.FileTaskStore"
description: "Persist inbound A2A tasks so a suspended request survives a restart. — Java."
sidebar:
  label: "A2AServer.FileTaskStore"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Java</strong> · package <code>io.github.muthuishere:toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7B</a>
</p>

```java
{/* TODO:signature */}
```

Persist inbound A2A tasks so a suspended request survives a restart.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```java
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```java
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```java
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `FileTaskStore` | [→](/api/javascript/serve/task-store/) |
| Python | `FileTaskStore` | [→](/api/python/serve/task-store/) |
| Go | `NewFileTaskStore` | [→](/api/go/serve/task-store/) |
| C# | `A2AServer.FileTaskStore` | [→](/api/csharp/serve/task-store/) |
| Elixir | `Toolnexus.Serve task_store` | [→](/api/elixir/serve/task-store/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`A2AServer.start`](/api/java/serve/a2a/) — Publish an Agent Card and answer JSON-RPC over the client loop — your toolkit becomes someone else's remote agent.
- [`A2AServer.buildAgentCard`](/api/java/serve/agent-card/) — Construct the Agent Card that advertises your name, skills and endpoint.
- [`McpServe.build`](/api/java/serve/mcp/) — The inbound MCP profile: any MCP client can call your tools.
