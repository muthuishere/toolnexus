---
title: "BuiltinTools.select"
description: "Pick which built-in file/shell/web tools are in play, by name or by source toggle. — Java."
sidebar:
  label: "BuiltinTools.select"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Java</strong> · package <code>io.github.muthuishere:toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §4A</a>
</p>

```java
{/* TODO:signature */}
```

Pick which built-in file/shell/web tools are in play, by name or by source toggle.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```java
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```java
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```java
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `selectBuiltins` | [→](/api/javascript/builtins/select/) |
| Python | `select_builtins` | [→](/api/python/builtins/select/) |
| Go | `SelectBuiltins` | [→](/api/go/builtins/select/) |
| C# | `BuiltinTools.Select` | [→](/api/csharp/builtins/select/) |
| Elixir | `Toolnexus.Builtin.load` | [→](/api/elixir/builtins/select/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`BuiltinTools.create`](/api/java/builtins/create/) — Construct every built-in tool directly, outside a toolkit.
