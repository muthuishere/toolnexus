---
title: "SkillSource.load"
description: "Glob a skills directory and expose one skill tool with progressive disclosure. — Java."
sidebar:
  label: "SkillSource.load"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Java</strong> · package <code>io.github.muthuishere:toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §3</a>
</p>

```java
{/* TODO:signature */}
```

Glob a skills directory and expose one skill tool with progressive disclosure.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```java
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```java
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```java
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `loadSkills` | [→](/api/javascript/skills/load/) |
| Python | `load_skills` | [→](/api/python/skills/load/) |
| Go | `LoadSkills` | [→](/api/go/skills/load/) |
| C# | `SkillSource.Load` | [→](/api/csharp/skills/load/) |
| Elixir | `Toolnexus.Skill.load` | [→](/api/elixir/skills/load/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`SkillSource.loadWith`](/api/java/skills/load-with/) — Supply skills as data or from a lazy provider, and narrow them per agent with an allowlist.
- [`SkillSource.listSkills`](/api/java/skills/list/) — Enumerate discovered skills and, crucially, the ones that were skipped and why.
