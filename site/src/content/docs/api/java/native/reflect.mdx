---
title: "@Tool annotation"
description: "Derive the schema from the function signature or annotation instead of writing it by hand. — Java."
sidebar:
  label: "@Tool annotation"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Java</strong> · package <code>io.github.muthuishere:toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §6</a>
</p>

```java
{/* TODO:signature */}
```

Derive the schema from the function signature or annotation instead of writing it by hand.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```java
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```java
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```java
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `tool` | [→](/api/javascript/native/reflect/) |
| Python | `tool` | [→](/api/python/native/reflect/) |
| Go | `NativeToolReflect` | [→](/api/go/native/reflect/) |
| C# | `ToolMethodAttribute` | [→](/api/csharp/native/reflect/) |
| Elixir | `Toolnexus.Native.reflect` | [→](/api/elixir/native/reflect/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`NativeTool.of`](/api/java/native/define-tool/) — Wrap a plain function with a name, description and schema — the shortest path from code you have to a tool the LLM can call.
- [`Tools.collect`](/api/java/native/collect/) — Sweep a module or class and collect every function marked as a tool.
