---
title: "agents.Agent"
description: "Define a sub-agent with its own toolkit, prompt and budget, callable as a tool by its parent. — JavaScript."
sidebar:
  label: "agents.Agent"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>JavaScript</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7D</a>
</p>

```ts
{/* TODO:signature */}
```

Define a sub-agent with its own toolkit, prompt and budget, callable as a tool by its parent.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```ts
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```ts
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```ts
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| Python | `Agent` | [→](/api/python/runtime/agent/) |
| Go | `agents.New` | [→](/api/go/runtime/agent/) |
| Java | `agents.Agent` | [→](/api/java/runtime/agent/) |
| C# | `Agent` | [→](/api/csharp/runtime/agent/) |
| Elixir | `Toolnexus.Agents` | [→](/api/elixir/runtime/agent/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`agents.AgentRuntime`](/api/javascript/runtime/runtime/) — The six host verbs that drive sub-agents, plus the read-only list and inspect views.
- [`agents.Handle`](/api/javascript/runtime/handle/) — The state machine for one spawned agent: pending, running, suspended, done.
- [`agents.Budget`](/api/javascript/runtime/budgets/) — Cap tool calls and wall-clock per agent and per team, enforced while the run is in flight.
- [`agents.taskTool`](/api/javascript/runtime/task-tool/) — The opt-in tool that lets the model itself spawn a teammate. Default OFF.
