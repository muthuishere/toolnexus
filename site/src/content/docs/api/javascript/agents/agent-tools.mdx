---
title: "agentTools"
description: "Expand a remote agent card into one tool per advertised skill. — JavaScript."
sidebar:
  label: "agentTools"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>JavaScript</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7A</a>
</p>

```ts
{/* TODO:signature */}
```

Expand a remote agent card into one tool per advertised skill.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```ts
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```ts
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```ts
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| Python | `agent_tools` | [→](/api/python/agents/agent-tools/) |
| Go | `AgentTools` | [→](/api/go/agents/agent-tools/) |
| Java | `A2A.agentTools` | [→](/api/java/agents/agent-tools/) |
| C# | `A2A.AgentTools` | [→](/api/csharp/agents/agent-tools/) |
| Elixir | `Toolnexus.Agents.tools` | [→](/api/elixir/agents/agent-tools/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`agent`](/api/javascript/agents/agent/) — Point at a remote agent's card and use it exactly like a local tool.
- [`parseAgentsConfig`](/api/javascript/agents/parse-config/) — Declare remote peers in config the way MCP servers are declared, with precedence rules.
