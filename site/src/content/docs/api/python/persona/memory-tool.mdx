---
title: "memory_tool"
description: "The opt-in built-in that lets a persona write durable notes to its own home. — Python."
sidebar:
  label: "memory_tool"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Python</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7E</a>
</p>

```python
{/* TODO:signature */}
```

The opt-in built-in that lets a persona write durable notes to its own home.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```python
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```python
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```python
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `memoryTool` | [→](/api/javascript/persona/memory-tool/) |
| Go | `MemoryTool` | [→](/api/go/persona/memory-tool/) |
| Java | `Home.memoryTool` | [→](/api/java/persona/memory-tool/) |
| C# | `Home.MemoryTool` | [→](/api/csharp/persona/memory-tool/) |
| Elixir | `Toolnexus.Agents.Home.memory_tool` | [→](/api/elixir/persona/memory-tool/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`compose_soul`](/api/python/persona/compose-soul/) — Build a persona's system prompt from its home directory: identity, memory, skills.
- [`agent_from_dir`](/api/python/persona/from-dir/) — Point at an agent home directory and get a configured agent back.
