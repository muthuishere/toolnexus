---
title: "create_builtin_tools"
description: "Construct every built-in tool directly, outside a toolkit. — Python."
sidebar:
  label: "create_builtin_tools"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Python</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §4A</a>
</p>

```python
{/* TODO:signature */}
```

Construct every built-in tool directly, outside a toolkit.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```python
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```python
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```python
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `createBuiltinTools` | [→](/api/javascript/builtins/create/) |
| Go | `CreateBuiltinTools` | [→](/api/go/builtins/create/) |
| Java | `BuiltinTools.create` | [→](/api/java/builtins/create/) |
| C# | `BuiltinTools.Create` | [→](/api/csharp/builtins/create/) |
| Elixir | `Toolnexus.Builtin.create` | [→](/api/elixir/builtins/create/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`select_builtins`](/api/python/builtins/select/) — Pick which built-in file/shell/web tools are in play, by name or by source toggle.
