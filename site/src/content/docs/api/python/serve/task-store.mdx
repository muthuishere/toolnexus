---
title: "FileTaskStore"
description: "Persist inbound A2A tasks so a suspended request survives a restart. — Python."
sidebar:
  label: "FileTaskStore"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Python</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7B</a>
</p>

```python
{/* TODO:signature */}
```

Persist inbound A2A tasks so a suspended request survives a restart.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```python
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```python
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```python
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `FileTaskStore` | [→](/api/javascript/serve/task-store/) |
| Go | `NewFileTaskStore` | [→](/api/go/serve/task-store/) |
| Java | `A2AServer.FileTaskStore` | [→](/api/java/serve/task-store/) |
| C# | `A2AServer.FileTaskStore` | [→](/api/csharp/serve/task-store/) |
| Elixir | `Toolnexus.Serve task_store` | [→](/api/elixir/serve/task-store/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolkit.serve`](/api/python/serve/a2a/) — Publish an Agent Card and answer JSON-RPC over the client loop — your toolkit becomes someone else's remote agent.
- [`build_agent_card`](/api/python/serve/agent-card/) — Construct the Agent Card that advertises your name, skills and endpoint.
- [`build_mcp_server`](/api/python/serve/mcp/) — The inbound MCP profile: any MCP client can call your tools.
