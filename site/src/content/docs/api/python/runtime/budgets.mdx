---
title: "Budget"
description: "Cap tool calls and wall-clock per agent and per team, enforced while the run is in flight. — Python."
sidebar:
  label: "Budget"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Python</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7D</a>
</p>

```python
{/* TODO:signature */}
```

Cap tool calls and wall-clock per agent and per team, enforced while the run is in flight.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```python
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```python
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```python
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `agents.Budget` | [→](/api/javascript/runtime/budgets/) |
| Go | `agents.Budget` | [→](/api/go/runtime/budgets/) |
| Java | `agents.Budget` | [→](/api/java/runtime/budgets/) |
| C# | `AgentTypes.Budget` | [→](/api/csharp/runtime/budgets/) |
| Elixir | `Toolnexus.Agents budget` | [→](/api/elixir/runtime/budgets/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Agent`](/api/python/runtime/agent/) — Define a sub-agent with its own toolkit, prompt and budget, callable as a tool by its parent.
- [`AgentRuntime`](/api/python/runtime/runtime/) — The six host verbs that drive sub-agents, plus the read-only list and inspect views.
- [`Handle`](/api/python/runtime/handle/) — The state machine for one spawned agent: pending, running, suspended, done.
