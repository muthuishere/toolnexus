---
title: "Toolnexus.Builtin.create"
description: "Construct every built-in tool directly, outside a toolkit. — Elixir."
sidebar:
  label: "Toolnexus.Builtin.create"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §4A</a>
</p>

```elixir
{/* TODO:signature */}
```

Construct every built-in tool directly, outside a toolkit.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `createBuiltinTools` | [→](/api/javascript/builtins/create/) |
| Python | `create_builtin_tools` | [→](/api/python/builtins/create/) |
| Go | `CreateBuiltinTools` | [→](/api/go/builtins/create/) |
| Java | `BuiltinTools.create` | [→](/api/java/builtins/create/) |
| C# | `BuiltinTools.Create` | [→](/api/csharp/builtins/create/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Builtin.select`](/api/elixir/builtins/select/) — Pick which built-in file/shell/web tools are in play, by name or by source toggle.
