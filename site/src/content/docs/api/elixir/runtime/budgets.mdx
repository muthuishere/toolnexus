---
title: "Toolnexus.Agents budget"
description: "Cap tool calls and wall-clock per agent and per team, enforced while the run is in flight. — Elixir."
sidebar:
  label: "Toolnexus.Agents budget"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7D</a>
</p>

```elixir
{/* TODO:signature */}
```

Cap tool calls and wall-clock per agent and per team, enforced while the run is in flight.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `agents.Budget` | [→](/api/javascript/runtime/budgets/) |
| Python | `Budget` | [→](/api/python/runtime/budgets/) |
| Go | `agents.Budget` | [→](/api/go/runtime/budgets/) |
| Java | `agents.Budget` | [→](/api/java/runtime/budgets/) |
| C# | `AgentTypes.Budget` | [→](/api/csharp/runtime/budgets/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Agents`](/api/elixir/runtime/agent/) — Define a sub-agent with its own toolkit, prompt and budget, callable as a tool by its parent.
- [`Toolnexus.Agents.Runtime`](/api/elixir/runtime/runtime/) — The six host verbs that drive sub-agents, plus the read-only list and inspect views.
- [`Toolnexus.Agents.Handle`](/api/elixir/runtime/handle/) — The state machine for one spawned agent: pending, running, suspended, done.
