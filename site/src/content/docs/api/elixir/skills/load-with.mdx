---
title: "Toolnexus.Skill.load"
description: "Supply skills as data or from a lazy provider, and narrow them per agent with an allowlist. — Elixir."
sidebar:
  label: "Toolnexus.Skill.load"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §3</a>
</p>

```elixir
{/* TODO:signature */}
```

Supply skills as data or from a lazy provider, and narrow them per agent with an allowlist.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `loadSkills` | [→](/api/javascript/skills/load-with/) |
| Python | `load_skills` | [→](/api/python/skills/load-with/) |
| Go | `LoadSkillsWith` | [→](/api/go/skills/load-with/) |
| Java | `SkillSource.loadWith` | [→](/api/java/skills/load-with/) |
| C# | `SkillSource.LoadWith` | [→](/api/csharp/skills/load-with/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Skill.load`](/api/elixir/skills/load/) — Glob a skills directory and expose one skill tool with progressive disclosure.
- [`Toolnexus.Skill.list`](/api/elixir/skills/list/) — Enumerate discovered skills and, crucially, the ones that were skipped and why.
