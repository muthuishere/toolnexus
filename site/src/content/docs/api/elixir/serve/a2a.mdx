---
title: "Toolnexus.Serve.start"
description: "Publish an Agent Card and answer JSON-RPC over the client loop — your toolkit becomes someone else's remote agent. — Elixir."
sidebar:
  label: "Toolnexus.Serve.start"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7B</a>
</p>

```elixir
{/* TODO:signature */}
```

Publish an Agent Card and answer JSON-RPC over the client loop — your toolkit becomes someone else's remote agent.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `startA2AServer` | [→](/api/javascript/serve/a2a/) |
| Python | `Toolkit.serve` | [→](/api/python/serve/a2a/) |
| Go | `StartAgent` | [→](/api/go/serve/a2a/) |
| Java | `A2AServer.start` | [→](/api/java/serve/a2a/) |
| C# | `A2AServer.Start` | [→](/api/csharp/serve/a2a/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Serve.build_agent_card`](/api/elixir/serve/agent-card/) — Construct the Agent Card that advertises your name, skills and endpoint.
- [`Toolnexus.Serve task_store`](/api/elixir/serve/task-store/) — Persist inbound A2A tasks so a suspended request survives a restart.
- [`Toolnexus.McpServe.build`](/api/elixir/serve/mcp/) — The inbound MCP profile: any MCP client can call your tools.
