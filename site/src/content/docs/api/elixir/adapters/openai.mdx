---
title: "Toolnexus.Adapters.to_openai"
description: "Render the toolkit as OpenAI function-calling schema. — Elixir."
sidebar:
  label: "Toolnexus.Adapters.to_openai"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §4</a>
</p>

```elixir
{/* TODO:signature */}
```

Render the toolkit as OpenAI function-calling schema.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `toOpenAI` | [→](/api/javascript/adapters/openai/) |
| Python | `to_openai` | [→](/api/python/adapters/openai/) |
| Go | `ToOpenAI` | [→](/api/go/adapters/openai/) |
| Java | `Adapters.toOpenAI` | [→](/api/java/adapters/openai/) |
| C# | `Adapters.ToOpenAI` | [→](/api/csharp/adapters/openai/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Adapters.to_anthropic`](/api/elixir/adapters/anthropic/) — Render the toolkit as Anthropic tool-use schema.
- [`Toolnexus.Adapters.to_gemini`](/api/elixir/adapters/gemini/) — Render the toolkit as Gemini function-declaration schema.
