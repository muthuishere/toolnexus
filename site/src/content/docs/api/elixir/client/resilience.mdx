---
title: "Toolnexus.Client.create"
description: "Classify an LLM error into retry / fail, bound a run with a deadline, and cancel cleanly. — Elixir."
sidebar:
  label: "Toolnexus.Client.create"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §8</a>
</p>

```elixir
{/* TODO:signature */}
```

Classify an LLM error into retry / fail, bound a run with a deadline, and cancel cleanly.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `ErrorInfo` | [→](/api/javascript/client/resilience/) |
| Python | `ErrorClassifier` | [→](/api/python/client/resilience/) |
| Go | `ErrorInfo` | [→](/api/go/client/resilience/) |
| Java | `LlmClient.ErrorInfo` | [→](/api/java/client/resilience/) |
| C# | `LlmClient.ErrorInfo` | [→](/api/csharp/client/resilience/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Client.create`](/api/elixir/client/create/) — The unified client: system prompt, skills injection, parallel and chained tool calls, retries, memory.
- [`Toolnexus.Client.run`](/api/elixir/client/run/) — Send a prompt, let the loop call tools until the model stops, get a RunResult.
- [`Toolnexus.Client.stream`](/api/elixir/client/stream/) — The streaming loop: text deltas, tool-call events, and suspension events as they happen.
- [`Toolnexus.Client.create`](/api/elixir/client/hooks/) — Intercept before/after model calls and tool calls: audit, redact, veto, or rewrite.
