---
title: "authRequired — suspend for authorization"
description: "The auth-shaped suspension: hand back a URL, resume once the user has granted access. — Elixir."
sidebar:
  label: "suspension / auth-required"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §10</a>
</p>

<Aside type="caution" title="Not available in Elixir">
Elixir ships no auth_required/pending producer helpers — build %ToolResult\{metadata: %\{pending: %Request\{\}\}\} by hand. Only the reader, ToolResult.pending?/1, exists.
</Aside>

The auth-shaped suspension: hand back a URL, resume once the user has granted access.

## What to use instead

{/* TODO:why */}
_TODO: the closest thing this port does offer, and how to get the same outcome._

## The same capability in the other ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `authRequired` | [→](/api/javascript/suspension/auth-required/) |
| Python | `auth_required` | [→](/api/python/suspension/auth-required/) |
| Go | `AuthRequired` | [→](/api/go/suspension/auth-required/) |
| Java | `Request.authRequired` | [→](/api/java/suspension/auth-required/) |
| C# | `Suspension.AuthRequired` | [→](/api/csharp/suspension/auth-required/) |

## See also

- [`Toolnexus.Client.create`](/api/elixir/suspension/wait-for/) — The single hook where the host resolves a suspension — in-process prompt or durable queue, same contract.
- [`Toolnexus.Client.resume`](/api/elixir/suspension/resume/) — The answer-carrying entry point: resume a parked run in a different process.
