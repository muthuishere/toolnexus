---
title: "Annotation / reflection tools"
description: "Derive the schema from the function signature or annotation instead of writing it by hand. — Elixir."
sidebar:
  label: "native / reflect"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §6</a>
</p>

<Aside type="caution" title="Not available in Elixir">
Elixir has no reflection-based tool builder; use Toolnexus.Native.define_tool/1 with an explicit input_schema.
</Aside>

Derive the schema from the function signature or annotation instead of writing it by hand.

## What to use instead

{/* TODO:why */}
_TODO: the closest thing this port does offer, and how to get the same outcome._

## The same capability in the other ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `tool` | [→](/api/javascript/native/reflect/) |
| Python | `tool` | [→](/api/python/native/reflect/) |
| Go | `NativeToolReflect` | [→](/api/go/native/reflect/) |
| Java | `@Tool annotation` | [→](/api/java/native/reflect/) |
| C# | `ToolMethodAttribute` | [→](/api/csharp/native/reflect/) |

## See also

- [`Toolnexus.Native.define_tool`](/api/elixir/native/define-tool/) — Wrap a plain function with a name, description and schema — the shortest path from code you have to a tool the LLM can call.
