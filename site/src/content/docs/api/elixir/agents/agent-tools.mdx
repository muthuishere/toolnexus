---
title: "Toolnexus.Agents.tools"
description: "Expand a remote agent card into one tool per advertised skill. — Elixir."
sidebar:
  label: "Toolnexus.Agents.tools"
---

import { Aside } from '@astrojs/starlight/components';

<p class="api-crumb">
<strong>Elixir</strong> · package <code>toolnexus</code> · <a href="https://github.com/muthuishere/toolnexus/blob/main/SPEC.md">SPEC §7A</a>
</p>

```elixir
{/* TODO:signature */}
```

Expand a remote agent card into one tool per advertised skill.

## When to use it

{/* TODO:when */}
_TODO: the situation this entry point is for._

## Why this and not the alternative

{/* TODO:why */}
<Aside type="tip" title="TODO: when NOT to use it">
_TODO: name the alternative and when to prefer it._
</Aside>

## Examples

### 1. TODO — the smallest useful call

{/* TODO:example1 */}
```elixir
// TODO
```

### 2. TODO — the realistic case

{/* TODO:example2 */}
```elixir
// TODO
```

### 3. TODO — the full surface

{/* TODO:example3 */}
```elixir
// TODO
```

## The same call in the other five ports

| Port | Symbol | |
|---|---|---|
| JavaScript | `agentTools` | [→](/api/javascript/agents/agent-tools/) |
| Python | `agent_tools` | [→](/api/python/agents/agent-tools/) |
| Go | `AgentTools` | [→](/api/go/agents/agent-tools/) |
| Java | `A2A.agentTools` | [→](/api/java/agents/agent-tools/) |
| C# | `A2A.AgentTools` | [→](/api/csharp/agents/agent-tools/) |

Behavior is identical across ports by contract — same `examples/` fixtures, same output.
See [SPEC §0](https://github.com/muthuishere/toolnexus/blob/main/SPEC.md).

## See also

- [`Toolnexus.Agents.new`](/api/elixir/agents/agent/) — Point at a remote agent's card and use it exactly like a local tool.
- [`Toolnexus.Agents.parse_config`](/api/elixir/agents/parse-config/) — Declare remote peers in config the way MCP servers are declared, with precedence rules.
