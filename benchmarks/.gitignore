# Build artifacts / binaries — regenerated by the runners, not source.
build/
*.jar
*.class
__pycache__/
.venv/
venv/
run_toolnexus_go/run_toolnexus_go
run_toolnexus_go/bench
node_modules/
.gradle/
resilience/run_go/.bin_res_go
# .NET / C#
bin/
obj/
*.dll
# Elixir
_build/
deps/
*.beam
# Go module binaries built into runner dirs (source: main.go/go.mod/go.sum kept)
run_eino/run_eino
run_langchaingo/run_langchaingo
# Clojure runner: the cljgo AOT binary and both hosts' caches. `src/toolnexus`
# is a SYMLINK to the local port and IS tracked — it is what makes this runner
# measure the port in this tree rather than a published artifact.
run_toolnexus_clojure/bench
run_toolnexus_clojure/.cpcache/
run_toolnexus_clojure/build.lock.edn
